[package]
name = "gateway-internal"
version = "0.1.0"
edition = "2021"
description = "Internal shared models for the Rust gRPC Gateway."
license = "MIT"
repository = "https://github.com/IberoRust/grpc-gateway-rust"

[dependencies]
